server:
  port: 7091

service:
  locale: ru_RU

springdoc:
  api-docs:
    path: /service-docs
  packages-to-scan: ru.otus.spring

spring:
  application:
    name: stock-service

  data:
    mongodb:
      authentication-database: admin
      database: StockServiceTest
      host: localhost
      port: 0

  #  mongock:
  #    change-logs-scan-package:
  #      - ru.otus.spring.changelogs
  #    runner-type: initializingbean

  output:
    ansi:
      enabled: ALWAYS

#  cloud:
#    stream:
#      binders: (input.destination = ..)
#      input.group=///
#
#      kafka:
#        binder:
#          brokers: localhost:9092

logging:
  pattern: ${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{70}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}

  level:
    root: info
    com:
      netflix:
        eureka: OFF
        discovery: OFF

    org:
      springframework: info



#  file:
#    name: logs/mylog.log